# 📷 画像ファイルの使い方ガイド

## 📁 画像を配置する場所

### ✅ 方法1: `src/assets/images/` フォルダ（推奨）

**用途**: コンポーネント内でインポートして使用する画像
- ヒーロー画像
- アイコン
- ロゴ
- 製品画像など

**メリット**:
- ビルド時に最適化される
- ファイル名にハッシュが付く（キャッシュ管理が楽）
- TypeScriptの型チェックが効く
- 存在しないファイルはビルドエラーになる

**配置場所**:
```
src/assets/images/
  ├── hero-image.jpg
  ├── logo.png
  ├── feature-1.png
  ├── feature-2.png
  └── icons/
      ├── shield.svg
      └── ai.svg
```

**使い方**:
```tsx
import heroImage from '@/assets/images/hero-image.jpg';
import logo from '@/assets/images/logo.png';

function Component() {
  return (
    <div>
      <img src={heroImage} alt="Hero" />
      <img src={logo} alt="Logo" className="w-32" />
    </div>
  );
}
```

---

### 📂 方法2: `public/` フォルダ

**用途**: 絶対パスで参照する静的ファイル
- ファビコン
- OGP画像
- robots.txt
- マニフェストファイル

**メリット**:
- ビルド後もファイル名が変わらない
- HTMLから直接参照できる
- 外部ツールから参照しやすい

**配置場所**:
```
public/
  ├── favicon.ico
  ├── og-image.jpg
  ├── logo.svg
  └── images/
      ├── sample-1.jpg
      └── sample-2.jpg
```

**使い方**:
```tsx
function Component() {
  return (
    <div>
      {/* publicフォルダのファイルは / から始まる絶対パスで指定 */}
      <img src="/images/sample-1.jpg" alt="Sample" />
      <img src="/logo.svg" alt="Logo" />
    </div>
  );
}
```

---

## 🎨 TOPページに画像を追加する例

### ヒーローセクションに画像を追加

**ステップ1**: 画像を配置
```
src/assets/images/hero-background.jpg
```

**ステップ2**: `src/pages/Index.tsx` で使用
```tsx
import heroImage from '@/assets/images/hero-background.jpg';

// ヒーローセクション内
<section className="relative py-24 px-6 bg-gradient-to-br from-slate-50 via-blue-50/30 to-slate-50">
  {/* 背景画像 */}
  <div 
    className="absolute inset-0 opacity-10" 
    style={{ backgroundImage: `url(${heroImage})`, backgroundSize: 'cover' }}
  />
  {/* コンテンツ */}
  <div className="relative z-10">
    {/* ... */}
  </div>
</section>
```

---

### 機能セクションに画像を追加

**ステップ1**: 画像を配置
```
src/assets/images/
  ├── ai-feature.png
  ├── management-feature.png
  └── security-feature.png
```

**ステップ2**: コンポーネントで使用
```tsx
import aiFeature from '@/assets/images/ai-feature.png';
import managementFeature from '@/assets/images/management-feature.png';
import securityFeature from '@/assets/images/security-feature.png';

<Card className="p-8">
  <CardContent className="p-0">
    {/* 画像を追加 */}
    <img 
      src={aiFeature} 
      alt="AI自動判定" 
      className="w-full h-48 object-cover rounded-lg mb-4"
    />
    <h3 className="text-xl font-bold">AI自動判定</h3>
    <p>東京都の指針に基づき...</p>
  </CardContent>
</Card>
```

---

## 🖼️ 画像の最適化のヒント

### 推奨サイズ

| 用途 | 推奨サイズ | フォーマット |
|------|-----------|------------|
| ヒーロー背景 | 1920x1080px | JPG/WebP |
| 機能画像 | 800x600px | PNG/WebP |
| アイコン | 64x64px | SVG/PNG |
| ロゴ | 可変 | SVG |
| OGP画像 | 1200x630px | JPG |

### ファイル形式

- **SVG**: ロゴ、アイコン（拡大しても綺麗）
- **PNG**: 透過が必要な画像
- **JPG**: 写真（ファイルサイズ小）
- **WebP**: 最新ブラウザで高圧縮（推奨）

### 最適化ツール

- [TinyPNG](https://tinypng.com/) - PNG/JPG圧縮
- [Squoosh](https://squoosh.app/) - 画像圧縮・変換
- [SVGOMG](https://jakearchibald.github.io/svgomg/) - SVG最適化

---

## 📝 実装例：TOPページに画像を追加

### 例1: ヒーローセクションに背景画像

```tsx
import heroPattern from '@/assets/images/hero-pattern.svg';

<section 
  className="relative py-24 px-6"
  style={{
    backgroundImage: `url(${heroPattern})`,
    backgroundRepeat: 'no-repeat',
    backgroundPosition: 'center',
    backgroundSize: 'cover'
  }}
>
  <div className="relative z-10">
    {/* コンテンツ */}
  </div>
</section>
```

### 例2: 機能カードにイラストを追加

```tsx
import aiIllustration from '@/assets/images/ai-illustration.png';

<Card className="overflow-hidden">
  <div className="aspect-video relative">
    <img 
      src={aiIllustration} 
      alt="AI機能のイラスト"
      className="w-full h-full object-cover"
    />
  </div>
  <CardContent className="p-6">
    <h3>AI自動判定</h3>
    <p>説明文...</p>
  </CardContent>
</Card>
```

### 例3: ロゴを表示

```tsx
import logo from '@/assets/images/logo.svg';

<div className="flex items-center">
  <img src={logo} alt="CusHara Sentinel" className="h-8 w-auto" />
  <span className="ml-2 font-bold">CusHara Sentinel</span>
</div>
```

---

## ✅ まとめ

### 画像を追加する手順

1. **画像を配置**
   ```
   src/assets/images/ に画像ファイルを入れる
   ```

2. **インポート**
   ```tsx
   import myImage from '@/assets/images/my-image.jpg';
   ```

3. **使用**
   ```tsx
   <img src={myImage} alt="説明" />
   ```

### フォルダ構成の推奨

```
src/assets/images/
  ├── hero/           # ヒーローセクション用
  ├── features/       # 機能紹介用
  ├── icons/          # アイコン
  ├── logos/          # ロゴ
  └── backgrounds/    # 背景画像
```

画像を追加したら、お気軽にお知らせください！実装をサポートします。


